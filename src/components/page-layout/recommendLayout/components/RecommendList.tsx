import Card from '@/components/common/Card/Card';

export default function RecommendList() {
  return (
    <div>
      {/* {data?.map((item) => (
        <Card
          type="productList"
          key={item.id}
          img={item.img}
          href={item.href}
          price={item.price}
          scope={item.rating}
          review={item.review}
          title={item.nameEng}
          koTitle={item.name}
        />
      ))} */}
    </div>
  );
}
